import {createTheme} from "@mui/material";


export const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#702121'
        },
        background: {
            default: '#FFFBF5',
            paper: '#fff9f0'
        }
    }
});